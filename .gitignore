# SSH keys and credentials — must never be committed
.ssh/
*.pem
*_ed25519
*_rsa
id_*

# Shell / environment dotfiles (machine-specific, not part of the site)
.bash_history
.bashrc
.profile
.gitconfig

# Caches
.cache/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
